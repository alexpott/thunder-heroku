build:
  languages:
    - php
#  pre:
  post:
    - composer create-project thunder/thunder-develop -s dev --working-dir=./
    - composer drupal-scaffold
run:
  web: vendor/bin/heroku-php-apache2 docroot/
